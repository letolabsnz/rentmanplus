attrs==26.1.0
brother-ql==0.9.4
click==8.4.2
future==1.0.0
packbits==0.6
# Capped below 10 on purpose — brother_ql 0.9.4 calls the removed
# PIL.Image.ANTIALIAS constant (gone since Pillow 10), which crashes every
# print. 9.5.0 is the last 9.x release.
pillow==9.5.0
pyusb==1.3.1
